program mytest1;

x, k, i: integer;
y, a: real;
z: integer;
f: char;
b,t,h: boolean

begin
    begin
        read(x,y);
        write(sin(y),x+y,2*x);
        while (x < y) do
        begin
            x := x + 1
        end;
        write(x,y);

        if (x mod 2 = 0) then
            write('P')
        else
            write('I')
    end
end