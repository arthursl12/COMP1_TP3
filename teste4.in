program MultipleAndOr;

x, y: integer;
p, q, r, s, t, u: real

begin
    if (p < q) or (r < s) and (t < u) then
        x := x - 1
    ;
    if (p < q) and (r < s) or (t < u) then
        x := x - 2
    ;
    x := x + 1
end