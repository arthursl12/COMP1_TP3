program TesteBasico;

x: integer;
b: boolean

begin
    b := (1 = 2);
    b := b or (1 != 2);


    x := 1 + 1;

    x := x + 2;
    x := 3;
    while (x <= 5) do
        x := x + 1;
        write(x)
    end;
    write(x)
end